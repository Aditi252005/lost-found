<!DOCTYPE html>
<html>
<head>
  <title><%= item.title %> - Lost & Found</title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <div class="container mt-5 mb-4">
    <div class="card shadow">
      <div class="card-body show-card">
        <h2 class="card-title"><%= item.title %></h2>
        <p class="card-text"><strong>Type:</strong> <%= item.type.toUpperCase() %></p>
        <p class="card-text"><strong>Description: </strong><i>" <%= item.description %> "</i></p>
        <p class="card-text"><strong>Date:</strong> <%= new Date(item.date).toDateString() %></p>
        <p class="card-text"><strong>Location:</strong> <%= item.location %></p>
        <p class="card-text"><strong>Contact:</strong> <%= item.contact %></p>

        <% if (item.image) { %>
          <img src="<%= item.image %>" class="img-fluid mt-3" alt="Item image" style="max-width: 100%; max-height: 250px; object-fit: contain;">
        <% } %>

      </div>
      <div class="m-3">
        <a href="/" class="btn btn-secondary mt-4">← Back to Home</a>

      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
